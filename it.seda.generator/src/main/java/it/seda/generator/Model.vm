package #models;

import java.io.Serializable;

#foreach($package in $modelsPackages)
import $package
#end


public class #ModelName implements Serializable{

	
	#foreach($field in $modelFields)
      private $field.type $field.name;
    #end
	
	#foreach($field in $modelFields)
	        
      public  $field.type get#field.Name(){
       return this.$field.name;
      }
      
       public void set#field.Name($field.type $field.name){
       this.$field.name=$field.name;
      }
    #end
    
    
    public String toString() {
    
    return " Model#Name [
    #firstModelName=" + $firstModelName + "
    #foreach($field in $modelFieldsWithOutFirst)	
		, $field.name=" + $fiels.name + "
    #end
    ]";
    }
	
	
	

}
